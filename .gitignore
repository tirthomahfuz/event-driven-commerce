# Local environment / secrets files — NEVER commit real or local creds.
# Matches at any directory depth (no leading slash).
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Next.js (later phases)
node_modules/
.next/

# OS / editor cruft
.DS_Store
Thumbs.db
